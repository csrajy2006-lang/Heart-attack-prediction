# -*- coding: utf-8 -*-
"""Heartattack_deployment.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1sAANizXrbkrKg9K3qAPYD_XnsDWne2so
"""

import streamlit as st
import pandas as pd
import joblib

# Load model
model = joblib.load("rfc_model.pkl")

st.title("❤️ Heart Attack Prediction App")

st.write("Enter Patient Details:")

# Input fields
age = st.number_input("Age", min_value=0)
gender = st.number_input("Gender (0 = Female, 1 = Male)")
heart_rate = st.number_input("Heart rate")
sys_bp = st.number_input("Systolic blood pressure")
dia_bp = st.number_input("Diastolic blood pressure")
blood_sugar = st.number_input("Blood sugar")
ck_mb = st.number_input("CK-MB")
troponin = st.number_input("Troponin")
# Create dataframe
input_data = pd.DataFrame({
    "Age": [age],
    "Gender": [gender],
    "Heart rate": [heart_rate],
    "Systolic blood pressure": [sys_bp],
    "Diastolic blood pressure": [dia_bp],
    "Blood sugar": [blood_sugar],
    "CK-MB": [ck_mb],
    "Troponin": [troponin]
})

# Prediction button
if st.button("Predict"):
    prediction = model.predict(input_data)[0]

    if prediction == 1:
        st.error("⚠️ High Risk of Heart Attack")
    else:
        st.success("✅ Low Risk of Heart Attack")